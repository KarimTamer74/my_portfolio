import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color primaryBlueLight = Color(0xFF1E3A8A);
  static const Color primaryBlueDark = Color(
    0xFF3B82F6,
  ); // Primary blue for buttons and links
  static const Color accentTeal = Color(
    0xFF2DD4BF,
  ); // Teal for GitHub button and icons
  static const Color lightBg = Color(0xFFF8FAFC);
  static const Color lightCard = Colors.white;
  static const Color textPrimary = Color(0xFF0F172A); // Dark text
  static const Color textSecondary =
      Colors.white; // White text for Hero section
  static const Color textLightGrey = Color(0xFF64748B); // For descriptions
  static const Color lightText = Color(0xFF0F172A); // Main text color

  // Dark Mode Toggle Button
  static const Color darkModeButtonBg = Color(
    0xFF1E3A8A,
  ); // Or your preferred dark mode toggle color
  static const Color darkModeButtonIcon = Colors.white;
static const Color lightGrey = Color(0xFFF5F5F5);
static const Color darkGrey = Color(0xFF1A1A1A);

  // --- Dark Theme Colors ---
  static const Color darkBg = Colors.black87; // A deep, dark charcoal
  static const Color darkCard = Color(
    0xFF2D3748,
  ); // A slightly lighter dark for surfaces
  static const Color textDarkGrey = Color(
    0xFFA0AEC0,
  ); // Lighter grey for descriptions on dark bg
}
